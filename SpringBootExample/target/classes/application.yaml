app:
  private-key: 12345

logging:
  level:
    org:
      springframework:
        web: DEBUG

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:./test;DB_CLOSE_ON_EXIT=FALSE
